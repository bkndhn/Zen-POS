# ZenPOS Rules

- The application is strictly named "ZenPOS". Never use "Hotel Zen POS", "Hotel ZenPOS", or other variations in the UI, HTML metadata, or config files.
- The default Vercel deployment URL is "https://zen-pos.vercel.app". Ensure "hotel-" is not prefixed to the URL.
